# JWT Configuration Template
# Copy this file to application-secrets.properties and update the values
# IMPORTANT: The application-secrets.properties file should never be committed to version control

# JWT Secret Key - Generate a secure random key for production
# You can generate a secure key using: openssl rand -base64 64
jwt.secret=REPLACE_WITH_YOUR_SECRET_KEY_AT_LEAST_256_BITS

# JWT Token Expiration in milliseconds (default: 24 hours)
jwt.expiration=86400000
